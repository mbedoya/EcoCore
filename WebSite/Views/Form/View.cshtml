@model WebSite.Models.FormUIModel

@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>

<div class="form">
    @foreach (var item in Model.Fields)
    {
        Html.RenderAction("DisplayFormField", "Home", new { model = @item });
    }
</div>